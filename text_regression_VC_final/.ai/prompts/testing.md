# Testing Prompt

Xác định task thuộc:
- unit test;
- model-level test;
- integration test;
- full-pipeline test.

Chạy test phù hợp.

Báo cáo:

```text
Test:
Command:
Result:
Failures:
Root cause:
```

Không bỏ qua test fail bằng cách xóa test hoặc làm yếu assertion mà không có lý do chính đáng.
